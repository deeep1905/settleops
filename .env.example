# Optional — LLM diagnosis assist (labeled, bounded, never in remediation).
# Without any key the deterministic rules brain runs everything.
# Groq and Gemini have free tiers.
GROQ_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
